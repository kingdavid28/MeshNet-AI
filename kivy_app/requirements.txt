# Note: Kivy requires Python 3.8-3.12 (not 3.13+)
kivy>=2.1.0
kivymd>=1.1.0
requests>=2.31.0
aiohttp>=3.9.0
pytest>=7.4.0
pytest-asyncio>=0.21.0
plyer>=2.1.0  # For GPS and hotspot functionality on Android
pyjnius>=1.4.0  # Required for plyer on Android
flask>=2.3.0  # For local backend server
flask-cors>=4.0.0  # For CORS support
